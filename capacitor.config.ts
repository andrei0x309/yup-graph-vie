import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.yup.graph.vie',
  appName: 'yup-graph-vie',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
